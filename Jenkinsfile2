pipeline {
    agent any
    tools{
        maven 'Maven-frank-1'
    }
    
    stages{
        

        stage('Deploy'){
            steps {
                sh 'docker run --name websockerproject -p 8083:8080 -d frankwebsocket'
            }

            post {
                success {
                    echo '正在构建'
                }
            }
        }
    }
}


              